export class card  {
  header:string
  content:string
  footer:string

  constructor(header: string, content: string, footer:string) {
    this.header = header;
    this.content = content;
    this.footer = footer;
  }
}